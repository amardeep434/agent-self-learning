# SDD ledger — plan: /home/amardeep/claude-self-learning/.claude/worktrees/hnp/docs/superpowers/plans/2026-07-25-harness-neutral-persistence.md
Task 1: complete (commits b3f758a..3e6b418, review clean)
Task 1: minor (deferred): resolve_home returns a CWD-relative path when HOME unset and no other branch matches — should fail loudly (paths.py resolve_home final return)
Task 1: minor (deferred): Windows separator handling only truly verified by the windows-latest CI job (Task 8)
Task 1: minor (deferred): tests/test-paths.py line 1 multi-import style nit (inherited from plan text)
Task 2: fix round 1/5 (1 addressed: SL_CONFIG_FILE via paths.py; commits 8dcbdeb..320b60f)
Task 2: fix round 2/5 (1 addressed: literal fallbacks when python3 absent + regression test; commits 320b60f..384a319)
Task 2: complete (commits 3e6b418..384a319, review clean)
Task 2: minor (deferred): PRE-EXISTING — only SL_HOME/SL_CONFIG_FILE are in _sl_env_snapshot; a config file silently overrides pre-set env for SL_STATE_DIR/SL_SKILLS_DIR/SL_MEMORY_DIR/SL_LOG_DIR/SL_SEARCH_DB, breaking "env beats file" for those five (config.sh:7-11). Triage at final review.
Task 2: ruling: settings-hooks.json ~/.claude paths in hooks block are correct — that file is Claude Code's own adapter config; constraint binds Copilot/VS Code paths only.
Task 3: fix round 1/5 (8 addressed: trailing-newline regex bypass, RecursionError escape, TypeError escape, memory-entry cap+dupes, Windows reserved names, version bool/float, fence fallback, NUL bytes; tests 17->48, mutants 5/5 killed; commits 81b7545..0fa15f5)
Task 3: fix round 2/5 (1 addressed: ReDoS quadratic backtracking 26.1s -> 0.00078s via linear str.find scan + timing-assert regression test; tests 48->53; commits 0fa15f5..0a0ae8f)
Task 3: complete (commits 384a319..0a0ae8f, review clean)
Task 3: NOTE: the plan file's Task 3 code block is known-defective (it specified the vulnerable regex and weak tests). The repository is authoritative; do not restore plan code.
Task 3: minor (deferred): _MAX_FENCE_CANDIDATES=10 silently drops a valid proposal hidden behind 10 invalid fenced blocks — fail-closed, worth a code comment only.
Task 4: fix round 1/5 (6 addressed: UnicodeDecodeError exit-code collapse, hardlink content leak into store, missing regression tests for all deviations, Windows st_reparse_tag detection, 0600 policy comment, unbounded append cap; tests 7->16, mutants 7/8 killed; commits 9ed5405..3157475)
Task 4: complete (commits 0a0ae8f..3157475, review clean)
Task 4: NOTE: implementer found a real hole in the PLAN's confinement check — if root itself is a symlink, target.parent.resolve()==root.resolve() passes trivially. Plan code was insufficient; repo is authoritative.
Task 4: minor (deferred): UTF-8 decode guard in _read_existing has no dedicated test; behaviour still correct via broader except ValueError in main(). One-line test would close it.
Task 4: ruling: symlinked STORE ROOT is allowed by design (root comes from env, not proposal content); store-INTERNAL dirs must not be symlinks. Reviewer concurred.
Task 4: ruling: cross-device rename impossible (mkstemp stages inside destination dir) — no EXDEV risk.
Task 5: fix round 1/5 (1 addressed: restored --max-turns/--output-format on backgrounded reviewer, positional passing preserved, both mutation-tested; commits 050a106..1085b6a)
Task 5: complete (commits 3157475..1085b6a, review clean)
Task 5: NOTE: the plan's Step-1 test was VACUOUS — script exits early on missing turn_counter.json so the test never reached the spawn path. Implementer seeded a counter; mutation test confirms the fixed test genuinely depends on spawn+writer.
Task 5: NOTE: plan's spawn snippet dropped --max-turns (orphaning SL_REVIEW_MAX_TURNS, leaving background reviewer uncapped). Task 6 must not repeat this.
Task 6: implemented (commit a934cb3, 12 checks, RED confirmed first) — task review dispatched with review-88476ca..a934cb3.diff; result not yet recorded.
Task 6: FINDING (carry to final review): Copilot CLI has NO turn cap for headless -p runs (--max-autopilot-continues is interactive-only); only --max-ai-credits (soft, min 30). SL_REVIEW_MAX_TURNS deliberately NOT force-mapped. Copilot reviewer therefore has no hard model-loop bound while the Claude path does. Suggested follow-up: separate SL_COPILOT_REVIEW_MAX_CREDITS knob, default unset.
Task 6: complete (commits 88476ca..a934cb3, review clean — Spec OK, Approved, zero findings; mutation 7/12 checks fail when spawn neutered; full suite 14/14)
Task 6: ruling: no --output-format flag needed for Copilot (text is already its default, unlike the Claude path where the flag is load-bearing). Divergent positional scheme (shift 3 + "$@") is correct because COPILOT_ARGS is variable-length.
Task 6: FINDING (Important, for final review): Copilot reviewer has NO cost ceiling. Reviewer verified via `copilot help limits` that --max-ai-credits DOES apply to non-interactive -p runs (--max-autopilot-continues is interactive-only). Recommended follow-up: new knob SL_COPILOT_MAX_AI_CREDITS wired to --max-ai-credits, default 30 (the CLI minimum). Do NOT reuse SL_REVIEW_MAX_TURNS — no principled unit conversion.
Task 6: FINDING (Important, NEW — plan does not cover this): config/copilot-hooks.json invokes `bash ~/.claude/scripts/self-learning/copilot-session-review.sh`. The Copilot adapter's SCRIPT INSTALL LOCATION is still Claude-namespaced, so a Copilot-only install still creates and depends on ~/.claude. This plan removed ~/.claude from the store and from script contents, but not from install paths. Task 7's guard will NOT catch it (that test invokes the script directly from the repo). Needs a follow-up task: relocate installed scripts to a harness-neutral location and update both config/copilot-hooks.json and install.sh/install.ps1.
Task 7: complete (commits 7ad0fb1..0910a37, review clean — Spec OK, Approved, zero Critical/Important; mutations 5/5 behaved correctly incl. reviewer-devised content-comparison mutation; full suite 10 shell + 5 Python)
Task 7: ruling: install-path violation (Copilot hook invokes ~/.claude/scripts/self-learning/...) deliberately EXCLUDED from Task 7 and split into new Task 7b, so the review package stays small and the coupling is repaired in the same task that asserts on it. Implementer disclosed the partial vacuity honestly in both report and inline comments.
Task 7: deviations accepted as strengthenings: (1) bounded polling (50 x 0.2s) replacing the plan's immediate -f check, which raced Task 6's detached pipeline; (2) defensive PATH extension toward the real python3/jq that refuses any directory also exposing `claude` and hard-fails instead.
Task 7: minor (deferred): no `trap ... EXIT` for TMP_HOME/FAKE_BIN cleanup — matches the existing convention in test-session-review.sh:104 and test-copilot-session-review.sh:94; fragile only to future edits.
Task 7: NOTE: the claude-absence setup self-check is load-bearing and irreplaceable — its own deletion is undetectable by design (mutation (e) survived, correctly).
Task 7b: ADDED DURING EXECUTION (plan amended in 44209dc). Install paths were never migrated: install.sh hardcoded ${HOME}/.claude for every data dir, the config file and the script destination, and config/copilot-hooks.json shipped a literal ~/.claude script path. Task 7's guard could not catch this (it invokes the script from the repo).
Task 7b: fix round 1/5 (1 Critical addressed: install.sh never copied persist-proposal.py, and copied only lib/*.sh plus a hand-listed paths.py, so lib/proposal_schema.py was missing too — the installed review pipeline piped into a nonexistent writer, i.e. the original silent no-op reborn in the install path. Fixed by adding persist-proposal.py to SCRIPTS and replacing the special-cased paths.py copy with a generic lib/*.py loop; new e2e test pipes a proposal into the INSTALLED writer to prove imports resolve from the installed tree; commits 0d63f45..5167f4b)
Task 7b: complete (commits 44209dc..5167f4b, review clean — Spec OK, Approved; mutations 7/7 killed incl. two reviewer-devised: adapter-step-skips vacuity probe failed cleanly, and a wrong-but-existing hook path was caught. Independently verified by the reviewer: real sandboxed install creates no ~/.claude, --dry-run writes nothing, second run idempotent, seeded legacy ~/.claude/memory left byte-identical, uninstall safe with python3 absent. Suite 11 shell + 5 Python)
Task 7b: ruling: severity escalated on the implementer's own "out of scope, pre-existing" concern. It was in scope — install.sh is a file this task modifies, and shipping without it would make the branch's headline claim false for every real install.
Task 7b: ruling: removing install.sh's precondition that ~/.claude must already exist is CORRECT and intended — that requirement is the dependency the plan removes; a Copilot-only user must be able to install. Adjudicated in the implementer's favour before review.
Task 7b: ruling: harness-owned config file locations (~/.claude/settings.json, ~/.copilot/hooks/) are NOT violations — only the script path each config invokes must be neutral. Hook configs are templates (__SL_SCRIPTS_DIR__) substituted at install time because static JSON cannot call paths.py.
Task 7b: FINDING (Important, pre-existing, outside 7b's file list) -> becomes Task 7c: scripts/curator-run.sh:21-29, scripts/index-session.sh:11-13 and scripts/self-learning-health.sh:64-161 hardcode ${HOME}/.claude internally instead of resolving via config.sh/paths.py. Reviewer demonstrated live that a freshly-installed self-learning-health.sh reports EVERY check as "[FAIL] missing / Fix: Run install.sh" on a machine that had just installed successfully, and curator-run.sh would manage skills at a nonexistent path — silently doing nothing. Violates global constraint 4 (paths resolved in exactly one place).
Task 7c: ADDED DURING EXECUTION (plan amended in 612816e), from Task 7b's review finding.
Task 7c: complete (commit 612816e..c283fad, review clean — Spec OK, Approved, zero Critical/Important; mutations 6/6 killed incl. three reviewer-devised: backup-dir revert, print-FAIL-but-exit-0 vacuity trap, and redirecting the legitimate ~/.claude/projects source. Suite 12 shell + 5 Python)
Task 7c: ruling: two ~/.claude references are legitimate and MUST stay — index-session.sh SESSIONS_DIR (Claude Code's own transcript directory, the session SOURCE) and self-learning-health.sh SETTINGS_FILE (Claude Code's own config). Implementer correctly demoted the settings.json check to WARN with Claude-Code labelling, so a Copilot-only machine no longer sees a spurious FAIL. Because of this distinction a source grep cannot be the primary assertion; the test asserts on behaviour under a redirected AGENT_LEARNING_HOME.
Task 7c: NOTE: the exit-code-only assertion in test-script-paths.sh is individually vacuous (mutation e: printing FAIL lines while exiting 0 did not fire it) but is backstopped by content assertions in the same run, which did fire. Not exploitable; worth knowing if that test is ever refactored.
Task 7c: minor (deferred): scripts/self-learning-health.sh:171 uses `stat -c %Y` — GNU-only, breaks on macOS/BSD. PRE-EXISTING and untouched by this diff, but Task 8 adds a macos-latest CI job that WILL fail on it. Must be fixed in Task 8.
Task 7c: minor (deferred): index-session.sh's `-newer "$DB_PATH"` logic can miss pre-existing transcripts on the first run against a fresh store (pre-existing staleness bug, unrelated to path resolution).
Task 8: fix round 1/5 (2 Important addressed: (a) sl_iso_to_epoch had ZERO coverage — reviewer's `echo 0; return 0` stub left test-turn-counter.sh passing; now 10 unit assertions in test-config.sh + 2 caller-gate assertions in test-turn-counter.sh, expectations hardcoded not derived, TZ pinned explicitly in two cases; (b) no per-suite timeout — a sleep-300 suite hung the runner indefinitely; now run_with_timeout() feature-detects timeout/gtimeout, runs suites in their own process group via `set -m`, tags timeouts distinctly (124/137) as failures, default 120s (~40-60x the slowest suite). Minor: pipefail warning comment. Commits eae8969..3fa7d1f)
Task 8: ruling: REJECTED the reviewer's suggested fix for the timeout finding (a bare `timeout` wrapper) — stock macOS has no `timeout`, so it would have broken the macos-latest job this very task exists to make green. Required feature-detection with a gtimeout fallback and a LOUD warning when neither exists; a silent loss of protection is the pattern this project keeps fixing.
Task 8: complete (commits c283fad..3fa7d1f, review clean then all 3 fix findings verdicted ADDRESSED on scoped re-review; no new breakage; fix diff is tests-only, no production script touched)
Task 8: verified at controller level (safety classifier was unavailable on the fix-round agent): independently reproduced the stub mutation — 8 failing assertions in test-config.sh, 1 in test-turn-counter.sh — and restored clean. Full runner: 17 suites (12 shell + 5 Python) pass in ~17s.
Task 8: NOTE: the implementer's own "unproven" concern was resolved — the reviewer constructed the missing case (a suite that backgrounds a child then exits immediately, so no timeout ever fires) and the process-group sweep still reaped the orphan.
Task 8: minor (deferred): a negative non-UTC offset silently flipped to positive (-05:00 treated as +05:00, a 10-hour drift) SURVIVES mutation — the only non-UTC case tested is +05:30. Narrow: this repo's own writer always emits `Z`, so not production-facing today. One extra assertion closes it if another round touches that file.
Task 8: minor (deferred): `set -m` process-group behaviour under Windows Git Bash is unverified locally — first push must confirm.
Task 8: NOTE: found during Task 8 and fixed there — index-session.py and skill-lifecycle.py used PEP 604 `X | None` without `from __future__ import annotations`, a hard ImportError on Python 3.9; plus 8 GNU-only `date -Iseconds` sites and 3 `date -d` sites, and a Windows blocker (actions/setup-python provides only python.exe, no python3, which every script here assumes).
Task 8: NOTE (for final review): turn-counter.sh measures ~150ms/call against a <50ms target, unchanged by this diff. Cause is a `python3 paths.py` subprocess spawn in config.sh, present since Task 2. Out of scope for Task 8; triage at final review.
Task 8: UNVERIFIABLE UNTIL PUSH: all 6 CI matrix cells. macOS `stat -f %m` / `date -j -f` branches, the Windows python3 shim, and `choco install sqlite` are reasoned-about only.
Task 9: fix round 1/5 (2 Critical + 2 Important + 1 Minor addressed; commits 92f3c47..e6bf1e3)
Task 9: CRITICAL was the SECOND occurrence of "new file never added to install.sh" (Task 7b was persist-proposal.py + proposal_schema.py; this time doctor.sh). Both were caught by a human reading code, never by a test. Fixed the instance AND required a structural guard: tests/test-install-paths.sh now asserts every scripts/*.sh and scripts/*.py is either installed or in an explicit commented exemption list ("name|reason" strings, visible in diff). Only exemption: sync-coach-rules.sh (maintainer-only vendoring tool, needs gh, writes into the repo checkout). Mutation-verified with a dummy script.
Task 9: Important: self-learning-health.sh and doctor.sh gave CONTRADICTORY verdicts on the same stale hook config — health reported [PASS] because it substring-matched the script NAME and never checked the path, while doctor reported STALE. Worse than a gap: health was the installed one. Fixed by extracting shared sl_check_hook_fresh() into config.sh so they cannot disagree by construction; reviewer probed trailing slashes, name-only commands, ../ paths and mixed stale/fresh multi-entry lists and found agreement in every case.
Task 9: Important: a genuinely vacuous assertion in test-doctor.sh — the fixture's modulo arithmetic never emitted the string the assertion claimed was absent, so flipping doctor's `tail -n 5` to `head -n 5` (showing oldest failures, hiding today's) survived with all 24 assertions green. Fixture rebuilt to emit 8 distinct dates with assertions on BOTH sides of the tail boundary; mutation now kills 3 assertions.
Task 9: complete (commits 3fa7d1f..e6bf1e3, all 5 fix findings verdicted ADDRESSED on scoped re-review, no new breakage. Suite 18 suites / 13 shell + 5 Python. Installed doctor.sh verified running standalone from the installed tree with PATH=/usr/bin:/bin.)
Task 9: ruling: doctor's exit code covers non-writable paths and a non-empty persist-failures.log, but NOT a detected legacy store — making legacy detection fatal would turn every upgraded machine permanently red and train users to ignore the exit code. A --strict flag was proposed and deliberately deferred rather than adding scope.
Task 9: verified no hot-path regression from adding sl_check_hook_fresh() to config.sh (sourced by EVERY hook script on both harnesses): turn-counter.sh 130-143ms, consistent with the ~150ms pre-existing baseline.
Task 9: NOTE: tests/test-script-paths.sh (a Task 7c guard) had its fixture changed — legitimate and STRENGTHENING, not a weakening: the old fixture used a placeholder `.../turn-counter.sh` path that correctly reads STALE under the new shared check. Reviewer confirmed by reverting it and watching the guard fail.
Task 9: minor (deferred): sl_check_hook_fresh uses an unanchored `grep -qF` substring match, so a hook pointing at `<script>.bak` reads as fresh. Both tools share the bug so they still agree; wants an anchored match.
Task 9: minor (deferred): when python3 is absent, SL_SCRIPTS_DIR resolves empty in self-learning-health.sh and every hook then reports stale regardless of truth. Masked today by a hard [FAIL] for missing python3 on the same run; matters if freshness is ever consumed programmatically.
Task 9: minor (deferred): self-learning-health.sh still does not check Copilot's hook config at all — only doctor.sh does.
Task 10: complete (commit e6bf1e3..f11a870). README.md: storage resolution order, migration note, CLAUDE_REVIEW_ENABLED deprecation, doctor/persist-failures.log prominence, roadmap + compatibility tables tied to actual tests/run-all.sh coverage. CLAUDE.md: corrected roadmap, peers/no-Claude-dependency statement, fixed stale install-path claims. WIP banner deliberately left in place (resume pointer while branch is open).
Task 10: ruling: Task 10's review is FOLDED INTO the whole-branch final review rather than run separately — it is prose-only, and the final reviewer is being told explicitly to verify doc claims against the tree. Avoids a redundant cycle on the most expensive tier.
Task 10: NOTE: the brief's "34 merged commits" figure was WRONG and the implementer correctly refused to propagate it — actual state is 63 commits on the branch, 29 ahead of local main, ZERO merged. The stale figure came from the handoff doc; do not reuse it.
Task 10: implementer confirmed via `gh run list` / `gh workflow list` and `git show origin/main:.github/workflows/ci.yml` that CI has genuinely never run — the workflow file is not on origin/main at all. Documented as unverified, not passing.
Task 10: no code defects found during doc verification; all read code matched its documentation and tests.
=== WHOLE-BRANCH FINAL REVIEW (opus, over befd131..f11a870) — NO-GO ===
Pushed f11a870 and opened draft PR #2. CI ran for the FIRST time: ubuntu 3.9+3.13 PASS; macos 3.9+3.13 FAIL; windows 3.9+3.13 FAIL (6 of 18 suites). The matrix caught two real defects that six rounds of inspection-based review could not.
Final review CONFIRMED the headline fix works: drove BOTH harnesses end-to-end on a sandboxed real install with shims — Copilot path wrote memory + skill at 0600 with persist.log {"written":[...]}; Claude path appended USER.md, reset the counter, wrote last-session-end.
Write-path security: CLEAN. 18 schema attacks + 11 filesystem attacks all executed (not reasoned): traversal, absolute paths, backslashes, CON/COM1, NUL, fullwidth homoglyph, ADS, $-anchor newline bypass, 400KB bracket bomb, 100k-deep nesting, 100k fence openers, symlinks/hardlinks in both modes, read-only dirs, non-UTF-8, 1MiB cap. All rejected, all fast, no partial writes, no escapes, 0600 as documented. Zero findings against proposal_schema.py or the write phase.
persist-failures.log coverage GOOD: recorded invalid proposal (1), reviewer non-zero (7), SIGKILL mid-output (137), read-only store (2). Only the no-proposal case writes nothing — see I9.
CRITICAL C1: paths.py emits WindowsPath, so `paths.py all` gives C:\Users\... and every bash consumer treats backslashes as escapes. Windows uninstall printed "Uninstall complete." and LEFT THE ENTIRE STORE ON DISK.
CRITICAL C2: scripts/skill-lifecycle.py:36-40 still hardcodes ~/.claude/learned-skills and reads CLAUDE_LEARNED_SKILLS_DIR, never SL_SKILLS_DIR. Task 7c fixed the three SHELL scripts and stopped. Lifecycle transitions never run, exit 0. On an upgraded machine the curator would shutil.move/rmtree inside the LEGACY store while reporting on the new one.
CRITICAL C3: self-learning-health.sh REQUIRED_SCRIPTS omits persist-proposal.py, lib/proposal_schema.py, copilot-session-review.sh, doctor.sh. Deleting the writer from a good install still yields "Status: HEALTHY". install.sh tells users to run this to verify. The original PASS-while-broken gate, relocated.
CRITICAL C4 (cross-task seam, brief error in Task 4 that no isolated review could catch): persist-proposal.py writes skills_dir/<name>.md FLAT, but every consumer needs <name>/SKILL.md — inject-agents-md.py:50-54, curator-run.sh:171, skill-lifecycle.py:154-158. Proven: the skill the pipeline had just persisted was INVISIBLE to inject-agents-md.py. Nothing updates .usage.json either. Both reviewer prompts still describe the pre-inversion directory contract.
IMPORTANT I5 (Task 7b regression): config.sh:66 defaults SL_COACH_RULES_DIR to ${SL_HOME}/scripts/self-learning/coach-rules but install.sh:267 installs to ${SL_HOME}/scripts/coach-rules. Route A silently yields zero signals forever.
IMPORTANT I6: with python3 absent, config.sh's literal fallbacks (added in 384a319) ignore AGENT_LEARNING_HOME and XDG_DATA_HOME entirely — persist-failures.log lands in a phantom store. The fallback mirrored paths.py's DEFAULT, not its CHAIN.
IMPORTANT I7: sl_iso_to_epoch broken on macOS for any non-Z timestamp — sole cause of both macOS CI failures. All three strategies only parse %Y-%m-%dT%H:%M:%SZ; GNU date's leniency on Linux hid it and the tests encoded that leniency as the contract.
IMPORTANT I8: prompts say skill names match ^[a-z0-9][a-z0-9._-]*$ (dots OK); schema is [A-Za-z0-9][A-Za-z0-9_-]{0,63} (no dots). A model obeying the prompt gets the WHOLE proposal rejected, discarding valid memory entries with it. Each prompt contradicts itself six lines later.
IMPORTANT I9: the no-proposal outcome — the original defect's exact signature — is invisible to doctor. Byte-identical to "extraction broke".
IMPORTANT I10: CLAUDE.md still carries the stale WIP banner ("6 tasks complete", resume at Task 7), a wrong commit count, and claims Phase 5 done "including Windows support" plus "No CI run has ever executed".
IMPORTANT I11: the ~/.claude source guard covers only three SHELL scripts; skill-lifecycle.py (live bug), coach-signals.py and inject-agents-md.py are unguarded.
Triage of the 14 deferred minors: MUST-FIX 1 (resolve_home CWD-relative when HOME unset), 3 (_sl_env_snapshot missing five vars), 7 (upgraded — it IS the macOS failure), 11 (health.sh scope); FOLD 10 into 3/I6; DEFER 2,4,5,6,8,9,12,13,14. Minor 8 retired: Windows CI ran run-all.sh to completion, so `set -m` works there. Minor 12 corrected: turn-counter is 58-62ms, not 130-150ms.
Controller plan: THREE SEQUENTIAL fix rounds (never parallel), then ONE scoped re-review over the whole fix range, then re-run CI.
=== SCOPED RE-REVIEW of fix rounds A/B/C (opus, f11a870..1ec4922) — NO-GO ===
(agent died once on a session limit mid-I11 and was resumed; tree was verified clean first — no mutation left applied)
ADDRESSED and verified by execution: C2 (incl. the upgraded-machine case — a forced destructive curator archived the NEW store and left ~/.claude byte-identical), I5, I6+minors 3/10, minor 1, C4 (full e2e: writer -> inject-agents-md + skill-lifecycle + curator all see the skill; .usage.json schema matches consumers; refresh preserves created_by/state/pinned/use_count; pre-existing flat <name>.md never deleted), I8, C3 (five distinct breakages incl. a PRESENT-but-unimportable proposal_schema.py, all reported, exit 1; self-check cannot touch the real store), minor 11, I9 (fires at 3 not 2; a single no-op is not alarming), doctor+no-python3, I10, M12, M13, M14, curator dead-code (re-derived by execution).
SECURITY: no regression from the directory-creating writer. 29 schema attacks, 22 filesystem attacks, 8 schema-BYPASS confinement probes (called _plan/_write_all directly with raw names), 40-iteration TOCTOU race swapping <name> between dir and symlink — 0/40 escapes. Modes 0600. All-or-nothing held in every mixed case. Round B's surviving mutation independently judged a non-gap (property doubly covered at current depth).
BLOCKER (a): I7 fix INCOMPLETE. scripts/skill-lifecycle.py:101-109 has its OWN copy of the ISO parser, untouched by round A. Two defects: (1) no Z swap -> ValueError on 3.9 -> None -> no anchor -> nothing archived -> exit 0 "Nothing to do" = EIGHTH instance of the pattern, and it regressed ubuntu 3.9 from PASS to FAIL; (2) no naive-datetime guard, so a tz-naive timestamp is read as LOCAL time (up to +/-14h drift) — live on every Python version, zero test coverage. Aggravated by mixed producers: shell emits Z, Python emits +00:00, both land in the same .usage.json.
BLOCKER (b): C1 NOT ADDRESSED. Round A's .as_posix() fixed backslash escaping but not the real cause — MSYS2 converts POSIX env values to Win32 form BEFORE native python.exe sees them, so /tmp/al arrives as C:\Users\...\Temp\al and .as_posix() faithfully echoes a Win32-flavoured path bash cannot compare against what it passed in. test-config.sh exit 127 also survives; reviewer's hypothesis is that round A's ln -s -> cp swap MADE IT WORSE (copied .exe files separate from their msys-2.0.dll siblings -> loader failure -> 127) and recommends forwarder shell scripts instead of copied binaries.
BLOCKER (c): I11 guard patterns too narrow — MUTATION-PROVEN that the guard would NOT have caught C2 itself. Pre-fix line was os.path.expanduser("~/.claude/learned-skills"); guard greps for '".claude"' only. Missed: single quotes, expanduser, unbraced $HOME/.claude, bare ~/.claude. Exemption list itself is honest (all four verified read-only).
DUPLICATED-LOGIC CLASS (the thing per-task review structurally cannot see): (1) 2 ISO parsers, fix landed in one — the confirmed 3.9 regression; (2) skill-layout literals restated in FIVE places with no shared definition — exactly what produced C4, so a sixth divergence is one edit away; (3) corrupt-.usage.json policy OPPOSITE in two components (persist-proposal refuses at exit 2; skill-lifecycle silently returns {} and says "No .usage.json found" at exit 0); (4) hook freshness correctly single-sourced via sl_check_hook_fresh — the pattern the other three should follow, and evidence the Task 9 extraction worked.
Minor/deferred from re-review: prompts/authoring-standards.md:13 still publishes the stale dot-inclusive regex (installed, but read by nothing at runtime); README's Windows row puts the Windows CI result in the Claude Code column and macOS in the Copilot column (columns are per-harness, not per-OS); _plan() reads .usage.json through a symlinked skills_dir BEFORE the symlink check (boolean is-valid-JSON oracle only, no disclosure); skill-lifecycle.py:137 creates .archive/ unconditionally so --dry-run is not fully read-only; curator report emits a stray bare "0" line. I5 caveat: all 45 coach rules LOAD but only 1 is evaluable by the narrow detect DSL (pre-existing, logged per-rule).
=== CI GREEN ON ALL SIX CELLS — run 30157000235 (commit f0574d8) ===
Progression: run1 (f11a870) ubuntu-only green -> run2 (1ec4922) macOS 3.13 green, ubuntu 3.9 REGRESSED, windows 6 suites failing -> run3 (2eb85cc) ubuntu x2 + macOS x2 green, windows 13 suites failing -> run4 (07ed30e) windows down to 3 suites -> run5 (f0574d8) ALL SIX GREEN, 28 suites each.
Fix round D (2eb85cc): extracted scripts/lib/isotime.py as the single parse_iso/now_iso and migrated skill-lifecycle.py, persist-proposal.py, index-session.py, coach-signals.py, with config.sh's fallback shelling out to it — the two-parser divergence is now structurally impossible, not patched. MSYS-form CLI output gated on MSYSTEM. ~/.claude guard widened from 2/6 to 6/6 mutation rows incl. the expanduser form that was C2's own literal. Corrupt-.usage.json policy unified (lifecycle now refuses at exit 2 instead of "Nothing to do"). Symlink-oracle hoist in _plan().
Fix round E (07ed30e): found a REAL product bug the re-review had not — paths.py's CLI never forced LF-only stdout, so Python's Windows text mode translated \n to \r\n even to a pipe, corrupting every path bash read via command substitution (explains test-install-paths.sh exit 5; reproduced locally, jq -r on malformed JSON exits 5). Fixed at source plus defensive \r-stripping in config.sh/install.sh/uninstall.sh. Shared tests/lib/path-compare.sh instead of nine pasted normalisation snippets. Agent honestly left 3 items unresolved rather than guessing.
Fix round F (f0574d8): test-paths.py failures were test ISOLATION — three tests monkeypatched the platform but inherited ambient MSYSTEM, which IS set on the real Windows runner, so production was right and the tests were wrong; fixed by patching os.environ in both directions. A 4th failure was the test's own ground truth being platform-dependent (str() of a drive-less WindowsPath). sl_canon_path was a THIRD divergent path rendering — fixed by adding a `canon` subcommand to paths.py and pointing the helper at it (one implementation, not a bash reimplementation).
SKIP DISCIPLINE (deliberate, and the right shape): 3 shell skips + 7 Python skips on Windows, each PRINTED with its reason and each gated on a PROBE that verifies the limitation (`[[ -L ]]` after ln -s; an actual write attempt after chmod 500) rather than on the platform name. A blanket `if windows: skip` would hide real Windows regressions forever. Consequence recorded in the docs: the symlink/hardlink write-path attack surface is exercised on Linux and macOS ONLY.
Docs updated (8ec073e) to record the green matrix AND disclose that Windows green is not equal coverage — the honest version, since "all six green" alone would overstate what Windows actually tested.
REMAINING GATE (human, cannot be automated): the live Copilot CLI end-to-end check — a real session, real model call, real file under the resolved memory directory. Every e2e run so far (mine, both reviewers', all six fix rounds) used a fake copilot shim. This is the exact scenario the branch exists to fix, so it should gate the PR leaving draft regardless of CI colour.
=== CORRECTION (fix-p3-toctou, 2026-07-25): the "40-iteration TOCTOU race ... 0/40 escapes" line above is WRONG ===
The scoped re-review entry above states "40-iteration TOCTOU race swapping <name> between dir and symlink — 0/40 escapes." That number was measured wrong and has been repeated as fact in this ledger since. The correction: a subsequently-codified version of the identical attack (tests/test-adversarial-sweep.py's TestTOCTOU, added in 2d84aa4) measured ~10% (later runs 12%) at the same 40-iteration count against the SAME unfixed writer, and an independent from-scratch reproduction (standalone harness, 60 iterations) measured 18.3% (11/60). Both are real; the original 0/40 figure was not reproduced and is now understood to have been an artifact of too few iterations against a race that needs sustained contention to manifest reliably — a busy-loop symlink-swap thread does not guarantee it wins the specific narrow window every iteration, so a small sample can plausibly land on zero even though the vulnerability is real and present. A wrong security number repeated as fact in this ledger is exactly the failure mode this project's own review process exists to catch; it should have been re-measured before being trusted, not carried forward.

FURTHER DETERMINATION (this pass): what escapes is FILE CONTENT, not empty directories. Sampled directly (see fix-p3-toctou-report.md): escaped entries outside the store were either the fully-committed SKILL.md (final proposal content) or a still-staged .persist-tmp-* file already carrying the proposal's real content -- across two independent 300-iteration measurements, zero escapes were empty directories. This is the more severe of the two possibilities named in the task brief: an arbitrary-write-as-user primitive, not merely a confinement leak.

FIX (this pass, commit range starting at this ledger entry): scripts/persist-proposal.py now performs the entire write phase (directory walk, target stat, staged-file create, final rename) through dir_fd-anchored syscalls (O_NOFOLLOW open/mkdir/stat/replace/unlink relative to an already-open directory fd) whenever DIR_FD_SUPPORTED — a real functional probe run at import time, not a platform-name check and not a bare os.supports_dir_fd lookup (which is itself demonstrably unreliable here: os.replace does not appear in os.supports_dir_fd on this project's own Linux host even though it accepts dir_fd and works correctly when called — see persist-proposal.py's _probe_dir_fd_support docstring). Measured post-fix: 0/300 escapes in a standalone harness, and 0/40 in the now-zero-tolerance TestTOCTOU (tests/test-adversarial-sweep.py). Mutation-tested: forcing DIR_FD_SUPPORTED False reproduces the race (15.3%/300 in the standalone harness; the codified mutation test in tests/test-persist-proposal.py's TestDirFdWritePath also asserts at least one escape in 60 iterations). Windows has no dir_fd support in the stdlib os module at all, so the pre-existing path-based writer (_write_all_path) remains the active path there with its TOCTOU residual disclosed in that function's docstring, the module docstring, and doctor.sh's new dir_fd probe section, rather than silently carried forward unstated. Full detail: fix-p3-toctou-report.md.
