# Auto-generated by the usage-limit hook when a session is about to be cut off.
# Transient resume scratch, superseded on every regeneration — not part of the
# reasoning record. The durable record is sdd/<plan>/ (tracked).
HANDOFF.md
